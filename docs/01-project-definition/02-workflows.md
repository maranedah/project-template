# Expected workflows

Read this when: defining features, or logging a workflow run in `../02-planning/tracking/workflow-runs.csv`.

Each workflow gets a stable `W-NN` id — features reference it in front-matter, and
completed runs are tracked against it.

## W-01 — <!-- FILL: workflow name -->

| | |
|---|---|
| Who | <!-- FILL: the user/role --> |
| Today (pain) | <!-- FILL: how it's done now, what it costs (time/errors) --> |
| With this solution | <!-- FILL: the improved flow, step by step --> |
| Improvement | <!-- FILL: time saved / errors avoided, estimated --> |

<!-- FILL: add W-02, W-03… as needed. One table each. -->
